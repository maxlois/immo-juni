<form method="post" action="{{ path('super_admin_location_delete', {'id': location.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette lacation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location.id) }}">
    <button class="btn">Delete</button>
</form>
